function Info() {
  const element = <h1>hello, werld</h1>;
  const user = {
    firstName: "Rauf",
    lastName: "RSS",
  };

  return <div>
    {user.firstName + " " + user.lastName}</div>;
}

export default Info;

//JSX - Расширение языка javascript.
//JSX - JS + XML;
//
